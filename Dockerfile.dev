FROM node:20-alpine
WORKDIR /app

# Speed up rebuilds
COPY package*.json ./
RUN npm install

# Copy source last for efficient caching
COPY . .

# Helpful for file-watching inside Docker on macOS/Windows
ENV WATCHPACK_POLLING=true
EXPOSE 3000

CMD ["npm", "run", "dev"]
